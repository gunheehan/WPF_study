<Page
    x:Class="WPF_study.Views.Pages.DataPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPF_study.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:WPF_study.Controls"
    xmlns:models="clr-namespace:WPF_study.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DataPage"
    d:DataContext="{d:DesignInstance local:DataPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="750"
    d:DesignWidth="850"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <controls:LoadingControl Grid.Column="0" HorizontalAlignment="Center"
                                 x:Name="searchGridLoadingControl" Visibility="Visible"/>
        
        <Grid Grid.Column="0" x:Name="searchGrid" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ui:TextBlock Grid.Row="0" x:Name="tbkId" Margin="0,0,0,10">
                <Run Text="ID" FontWeight="Bold" />
                <Run Text="*" Foreground="OrangeRed"/>
            </ui:TextBlock>

            <ui:NumberBox Grid.Row="1" Text="{Binding ViewModel.SelectedId, Mode=TwoWay}" Margin="0,0,0,10"/>

            <ui:TextBlock Grid.Row="2" x:Name="tbkAdminAgency" Margin="0,0,10,10">
                <Run Text="Administrative Agency" FontWeight="Bold" />
                <Run Text="*" Foreground="OrangeRed"/>
            </ui:TextBlock>

            <ComboBox Grid.Row="3" x:Name="cbxAdminAgency" 
                      Text="{Binding ViewModel.SelectedAdministrativeAgency}"
                      SelectedItem="{Binding ViewModel.SelectedAdministrativeAgency, Mode=TwoWay}"
                      SelectionChanged="cbxAdminAgency_SelectionChanged"
                      IsEditable="True"
                      ItemsSource="{Binding ViewModel.AdministrativeAgency, Mode=TwoWay}" Margin="0,0,0,10"/>

            <ui:TextBlock Grid.Row="4" x:Name="tbktotalPopulation" Margin="0,0,0,10">
                <Run Text="Totla Population" FontWeight="Bold" />
                <Run Text="*" Foreground="OrangeRed"/>
            </ui:TextBlock>
            <ui:NumberBox Grid.Row="5" x:Name="tbxtotalPopulation" Text="{Binding ViewModel.SelectedTotalPopulation, Mode=TwoWay}" Margin="0,0,0,10"/>

            <ui:TextBlock Grid.Row="6" x:Name="tbkMalePopulation" Margin="0,0,0,10">
                <Run Text="Male Population" FontWeight="Bold" />
                <Run Text="*" Foreground="OrangeRed"/>
            </ui:TextBlock>
            <ui:NumberBox Grid.Row="7" x:Name="tbxMalePopulation" Text="{Binding ViewModel.SelectedMalePopulation, Mode=TwoWay}" Margin="0,0,0,10"/>

            <ui:TextBlock Grid.Row="8" x:Name="tbkFemalePopulation" Margin="0,0,0,10">
                <Run Text="Female Population" FontWeight="Bold" />
                <Run Text="*" Foreground="OrangeRed"/>
            </ui:TextBlock>
            <ui:NumberBox Grid.Row="9" x:Name="tbxFemalePopulation" Text="{Binding ViewModel.SelectedFemalePopulation, Mode=TwoWay}" Margin="0,0,0,10"/>

            <ui:TextBlock Grid.Row="10" x:Name="tbkSexRatio" Margin="0,0,0,10">
                <Run Text="Sex Ratio" FontWeight="Bold" />
                <Run Text="*" Foreground="OrangeRed"/>
            </ui:TextBlock>
            <ui:NumberBox Grid.Row="11" Text="{Binding ViewModel.SelectedSexRatio, Mode=TwoWay}" Margin="0,0,0,10"/>

            <ui:TextBlock Grid.Row="12" x:Name="tbkNumnerOfHouseholds" Margin="0,0,0,10">
                <Run Text="Number Of Households" FontWeight="Bold" />
                <Run Text="*" Foreground="OrangeRed"/>
            </ui:TextBlock>
            <ui:NumberBox Grid.Row="13" Text="{Binding ViewModel.SelectedNumnerOfHouseholds, Mode=TwoWay}" Margin="0,0,0,10"/>

            <ui:TextBlock Grid.Row="14" x:Name="tbkNumverOfPeoplePerHouseHolds" Margin="0,0,0,10">
                <Run Text="Number Of People Per Households" FontWeight="Bold" />
                <Run Text="*" Foreground="OrangeRed"/>
            </ui:TextBlock>
            <ui:NumberBox Grid.Row="15" Text="{Binding ViewModel.SelectedNumverOfPeoplePerHouseHolds, Mode=TwoWay}" Margin="0,0,0,10"/>

            <ui:Button Grid.Row="16" Height="50" Width="300" Background="SteelBlue"
                       Command="{Binding ViewModel.CreateNewDataCommand}"
                       Content="Create" Foreground="White" Margin="0,0,0,10"/>

            <Grid Grid.Row="17">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="0" Height="50" Width="145" Background="SteelBlue" Command="{Binding ViewModel.ReadAllDataCommand}"
                           Content="Read All" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <ui:Button Grid.Column="1" Height="50" Width="145" HorizontalAlignment="Center" Background="SteelBlue" Command="{Binding ViewModel.ReadDetailDataCommand}"
                           Content="Read" Foreground="White" Margin="0,0,0,10"/>
            </Grid>

            <ui:Button Grid.Row="18" Height="50" Width="300" Background="Bisque" Command="{Binding ViewModel.UpdateDataCommand}"
                       Content="Update" Foreground="Black" Margin="0,0,0,10"/>

            <ui:Button Grid.Row="19" Height="50" Width="300" Background="IndianRed" Command="{Binding ViewModel.DeleteDataCommand}"
                       Content="Delete" Foreground="White" Margin="0,0,0,10"/>
        </Grid>
        
        <controls:LoadingControl Grid.Column="1" HorizontalAlignment="Center"
                                 x:Name="dgGridLoadingControl" Visibility="Visible"/>
        
        <Grid Grid.Column="1" x:Name="dgGrid" Visibility="Collapsed">
            <DataGrid x:Name="dgGangnamguPopulation" ItemsSource="{Binding ViewModel.GangnamguPopulations, Mode=OneWay}">
                
            </DataGrid>
        </Grid>
    </Grid>
</Page>
